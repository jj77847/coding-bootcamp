// returns an array of command line arguments
const args = process.argv;
console.log(process);

const arg1 = args[2];
const arg2 = args[3];

if (arg2) {
  // do arg2 stuff
}

console.log(arg1, arg2);
