const sum = (a, b) => a + b;

const difference = (a, b) => a - b;

const product = (a, b) => a * b;

const quotient = (a, b) => a / b;

module.exports = {
  sum: sum,
  difference: difference,
  product: product,
  quotient: quotient,
};
